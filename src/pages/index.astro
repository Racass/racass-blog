---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const base = `${import.meta.env.BASE_URL.replace(/\/$/, '')}/`;
---

<!doctype html>
<html lang="pt-BR">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main class="home">
			<section class="hero">
				<p class="eyebrow">Portfólio acadêmico · Ensino · Tecnologia</p>
				<h1>Ideias, pesquisa e recursos para aprender.</h1>
				<p class="introduction">
					Um espaço para trabalhos acadêmicos, materiais didáticos, artigos técnicos e projetos.
				</p>
				<div class="actions">
					<a class="primary" href={`${base}about/`}>Sobre mim</a>
					<a href={`${base}blog/`}>Ler artigos</a>
				</div>
			</section>
			<section class="areas" aria-label="Áreas">
				<article>
					<h2>Trabalho acadêmico</h2>
					<p>Pesquisas, publicações, apresentações e atividades profissionais.</p>
				</article>
				<article>
					<h2>Ensino</h2>
					<p>Materiais de aula, exercícios, referências e recursos para estudantes.</p>
				</article>
				<article>
					<h2>Artigos</h2>
					<p>Notas e textos práticos sobre tecnologia, educação e projetos atuais.</p>
				</article>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.home {
		width: 1080px;
		padding-top: 6rem;
	}
	.hero {
		max-width: 820px;
	}
	.eyebrow {
		margin-bottom: 1rem;
		color: var(--accent);
		font-size: 0.85rem;
		font-weight: 700;
		letter-spacing: 0.12em;
		text-transform: uppercase;
	}
	h1 {
		font-size: clamp(3rem, 8vw, 5.5rem);
		letter-spacing: -0.045em;
	}
	.introduction {
		max-width: 680px;
		color: rgb(var(--gray));
		font-size: 1.35rem;
	}
	.actions {
		display: flex;
		flex-wrap: wrap;
		gap: 1rem;
		margin-top: 2rem;
	}
	.actions a {
		padding: 0.65rem 1.1rem;
		border: 1px solid var(--accent);
		border-radius: 999px;
		text-decoration: none;
	}
	.actions .primary {
		background: var(--accent);
		color: white;
	}
	.areas {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.25rem;
		margin-top: 7rem;
	}
	.areas article {
		padding: 1.5rem;
		border: 1px solid rgb(var(--gray-light));
		border-radius: 18px;
		background: rgba(255, 255, 255, 75%);
	}
	.areas h2 {
		font-size: 1.35rem;
	}
	.areas p {
		margin: 0;
		color: rgb(var(--gray));
		font-size: 1rem;
	}
	@media (max-width: 720px) {
		.home {
			padding-top: 3rem;
		}
		.areas {
			grid-template-columns: 1fr;
			margin-top: 4rem;
		}
	}
</style>
